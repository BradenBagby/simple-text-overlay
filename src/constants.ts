import { BackgroundConfig, FontConfig } from './types';

export const DEFAULT_FONT_CONFOG: Required<FontConfig> = {
  name: 'Arial',
  size: 32,
  color: 'white',
  align: 'center',
  lineHeight: 1,
  path: '',
};
